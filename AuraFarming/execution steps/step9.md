# Step 9 - Market Price Scene

## Goal
Implement `market-price.fxml` for pricing, comparison, and history.

## Dependencies
- Step 8 completed.

## Build Tasks
1. Inputs: district, crop, price per kg.
2. Buttons: save, view latest, compare district, view history.
3. Farmer permissions: view-only (save disabled).
4. Right panel lists full history records.
5. Add date range filters (From/To).

## Done Criteria
- Market data supports update and historical analysis within role limits.